---
import LegalLayout from '../../layouts/LegalLayout.astro';
import { getApp, COMPANY } from '../../data/apps';

const app = getApp('workshop')!;
const updatedDate = 'August 31, 2026';
const sections = [
  { href: '#availability', label: 'Availability' },
  { href: '#identity', label: 'Sign-in and identity' },
  { href: '#sync-offline', label: 'Uploads and offline limits' },
  { href: '#analysis', label: 'URL analysis' },
  { href: '#export-backup', label: 'Export and backup' },
  { href: '#deletion', label: 'Account deletion' },
  { href: '#troubleshooting', label: 'Troubleshooting' },
  { href: '#contact', label: 'Contact' },
];
---

<LegalLayout
  app={app}
  title={`${app.name} Support — ${COMPANY.name}`}
  description="Help with the Workshop web workspace, Microsoft sign-in, uploads, URL analysis, export limits, recovery, and account deletion."
  path="/workshop/support"
  page="support"
  heading="Workshop Support"
  documentMeta={`Updated ${updatedDate} · Web workspace current`}
  summary="The online, account-backed web workspace at workshop.nintek.com is the sole supported Workshop client."
  sections={sections}
  evidenceNote="Support guidance was reviewed against the current Workshop web service and recovery runbook on August 31, 2026."
  theme="workshop"
>
  <section id="availability" class="legal-section">
    <h2>Current availability</h2>
    <p class="legal-callout">
      Use <a href="https://workshop.nintek.com">workshop.nintek.com</a> for the
      current Workshop service. It runs in the browser, requires a connection for
      account data, and offers a read-only demo before sign-in.
    </p>
  </section>

  <section id="identity" class="legal-section">
    <h2>Sign-in and account identity</h2>
    <p>
      The web workspace signs in with Microsoft. If projects appear missing,
      confirm that you used the same Microsoft account as before. Any existing
      Apple-backed Workshop account remains a separate workspace and is not
      automatically linked or merged with the Microsoft-backed workspace.
    </p>
    <p>
      Support can help identify the account path but will not ask for a password,
      verification code, Microsoft token, or encrypted authorization credential.
    </p>
  </section>

  <section id="sync-offline" class="legal-section">
    <h2>Uploads, sync, and offline limits</h2>
    <p>
      Workshop is online-only for account data. Projects, edits, cut lists,
      materials, shopping, logs, uploads, analysis, and account deletion require
      a working connection. The web workspace is not an installable or offline PWA
      and has no offline write queue.
    </p>
    <p>
      Unit-conversion tables are client-side calculations and can keep working in
      an already loaded page, but they are not an editable offline workspace or
      backup. Do not assume a project is safely on the server until its change or
      upload finishes without an error.
    </p>
    <p>
      If a photo or file does not appear, remain signed in to the correct Microsoft
      account, reconnect, reopen the project, and retry the upload once. Keep the
      original file until the account-backed copy is visible.
    </p>
  </section>

  <section id="analysis" class="legal-section">
    <h2>Project URL analysis</h2>
    <p>
      URL analysis is optional and runs only after you submit a public project
      page. Workshop may reject a private, local, or unsafe network destination. A
      provider outage or missing configuration returns an error rather than a
      fabricated draft.
    </p>
    <p>
      Review the imported parts, dimensions, materials, and instructions before
      saving or building. Workshop and its generated draft do not replace the
      source, safe tool practice, or qualified engineering advice.
    </p>
  </section>

  <section id="export-backup" class="legal-section">
    <h2>Export and backup limits</h2>
    <p>
      The Settings JSON download is a project-list summary. It omits project detail
      tables, account and authentication state, and uploads, and it cannot be
      imported as a restore. Keep original photos, drawings, and any independent
      build records you need.
    </p>
    <p>
      Workshop also maintains checksummed operator recovery bundles. Those backups
      are for service recovery, not a user-downloadable archive. Historical
      bundles can retain deleted data until scheduled expiry; if an older recovery
      point is restored, a human operator must reconcile later deletions before
      reopening the service. This step is required by the runbook and is not
      automated by the restore code.
    </p>
  </section>

  <section id="deletion" class="legal-section">
    <h2>Delete a Workshop account</h2>
    <p>
      In the web workspace, open <strong>Settings → Account</strong>, review the
      scope, and confirm account deletion. Deletion applies only to the current
      Microsoft-backed Workshop workspace. It does not delete the Microsoft
      account or a separate existing Apple-backed workspace.
    </p>
    <p>
      If Workshop cannot confirm deletion, it leaves the account data intact and
      reports a retryable failure. For help with a separate Apple-backed account,
      email <a href={`mailto:${COMPANY.privacyEmail}`}>{COMPANY.privacyEmail}</a>.
    </p>
    <p>
      Export the project summary first if it is useful, while remembering that it
      omits project details and uploads.
    </p>
  </section>

  <section id="troubleshooting" class="legal-section">
    <h2>Troubleshooting checklist</h2>
    <ol>
      <li>Confirm that the issue occurs in the current web workspace.</li>
      <li>Confirm the active Microsoft account before changing sessions.</li>
      <li>Reconnect and retry the failed save, upload, or analysis once.</li>
      <li>Keep the original file during a data-recovery issue.</li>
      <li>
        Record the browser, Workshop version, project name, time, and visible
        recovery message.
      </li>
    </ol>
    <p>
      Do not send credentials, private tokens, payment details, or a complete
      project archive unless support asks for a specific redacted sample.
    </p>
  </section>

  <section id="contact" class="legal-section">
    <h2>Contact and legal documents</h2>
    <p>
      Email <a href={`mailto:${COMPANY.email}`}>{COMPANY.email}</a> for product
      help. Read the <a href="/workshop/privacy">Workshop Privacy Policy</a> for
      storage, providers, backup retention, and deletion boundaries. Use of
      Workshop is subject to the <a href="/terms">Nintek Terms of Use</a>.
    </p>
  </section>
</LegalLayout>
