<figure class="tabloom-shot">
  <div class="tabloom-shot-ambient" aria-hidden="true"></div>
  <picture>
    <source
      srcset="/apps/tabloom/page-editor-960.webp 960w, /apps/tabloom/page-editor.webp 1876w"
      sizes="(max-width: 880px) min(480px, 92vw), 46vw"
      type="image/webp"
    />
    <img
      src="/apps/tabloom/page-editor.webp"
      alt="Tabloom's current page editor with private notebook content obscured"
      width="1876"
      height="970"
      loading="lazy"
      decoding="async"
    />
  </picture>
  <figcaption>Real capture · private content obscured</figcaption>
</figure>

<style>
  .tabloom-shot {
    position: relative;
    z-index: 1;
    margin: 0;
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: var(--r-lg);
    background: #171a20;
    box-shadow: var(--shadow-float);
  }

  .tabloom-shot-ambient {
    position: absolute;
    z-index: -1;
    inset: -42px;
    background: radial-gradient(circle at 60% 40%, rgba(59, 102, 245, 0.24), transparent 68%);
    filter: blur(24px);
  }

  .tabloom-shot picture,
  .tabloom-shot img {
    display: block;
    width: 100%;
  }

  .tabloom-shot img {
    height: auto;
    aspect-ratio: 1876 / 970;
    object-fit: cover;
  }

  .tabloom-shot figcaption {
    position: absolute;
    right: 12px;
    bottom: 10px;
    padding: 5px 9px;
    border: 1px solid rgba(244, 246, 248, 0.18);
    border-radius: 999px;
    background: rgba(23, 26, 32, 0.86);
    color: #f4f6f8;
    font-size: 0.68rem;
    font-weight: 600;
    box-shadow: 0 4px 14px rgba(23, 26, 32, 0.28);
  }

  @container (max-width: 360px) {
    .tabloom-shot img {
      aspect-ratio: 4 / 3;
      object-position: left center;
    }
  }
</style>
